cd ~/projects/current_projects/lem-in_github/libft
git add *
git commit -m "scripted push message, no really though this is a script"
git push
cd ../
git add sources includes test libft
git commit -m "scripted as well, for lem-in project"
git push
git status
echo "success???"
